#Generated by Eclipse Messages Editor (Eclipse Babel)

spring.datasource.password              = admin1234
spring.datasource.url                   = jdbc:mysql://aalonw7sz3az4h.cokcf7p9g\
                                          umc.ap-southeast-1.rds.amazonaws.com:\
                                          3306/inventory?useSSL=false
spring.datasource.username              = admin
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.show-sql                     = true
spring.profiles.active                  = embedded
spring.thymeleaf.mode                   = LEGACYHTML5
spring.jpa.properties.hibernate.id.new_generator_mappings=false

# ==============================================================
# = Spring Security / Queries for AuthenticationManagerBuilder
# ==============================================================
spring.queries.users-query=select username,password,active,name,last_name from user where username=?
spring.queries.roles-query=select u.username, r.role from user u inner join user_role ur on(u.user_id=ur.user_id) inner join role r on(ur.role_id=r.role_id) where u.username=?
